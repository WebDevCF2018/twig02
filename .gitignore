# phpstorm ignore
/.idea/
# don't ignore (!) vendor's folder
!vendor/
# ignore ALL (*) in vendor's folder
vendor/*
# don't ignore (!) twig's folder and autoload.php
!vendor/autoload.php
!vendor/twig/
